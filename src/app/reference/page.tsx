import ReferenceView from "./ReferenceView";

export const metadata = {
  title: "Quick Reference — Abbreviations, Ports, Subnetting",
  description:
    "IT certification quick reference: networking abbreviations, common ports and protocols, subnetting chart, and administrative distances.",
};

export default function ReferencePage() {
  return <ReferenceView />;
}
