import ProgressView from "./ProgressView";

export const metadata = {
  title: "Your Progress",
  description: "Track your accuracy, streaks, and per-domain performance across all certifications.",
};

export default function ProgressPage() {
  return <ProgressView />;
}
